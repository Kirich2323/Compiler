type
    rec = record
        x : integer;
        y : integer;
    end;

var
    p : rec;
    r : rec;
    a : array [1..10] of rec;
begin
    a[1].x := 10;
    a[1].y := 22;
    p := a[1];
    writeln(p.x);
    writeln(p.y);
end.